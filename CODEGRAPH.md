# DynamoDb.Fluent

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-08 11:52 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

DynamoDb.Fluent is a .NET library that provides a fluent, type-safe abstraction layer over Amazon DynamoDB. The repository contains two projects: the core library (DynamoDb.Fluent) and its test suite (DynamoDb.Fluent.Tests). The core library exposes a unified interface for common DynamoDB operations — Put, Delete, Find, Query, and index-based queries — and crucially includes a complete in-memory implementation of those same interfaces, enabling unit testing without a live AWS connection. The test project exercises both the in-memory implementation (for fast, isolated unit tests) and a real DynamoDB backend (for integration tests), validating correctness of the fluent API against both. The library is consumed as a package by at least one external repository ('galleries'), indicating it is published as a reusable NuGet package. The overall architecture separates the DynamoDB abstraction contract from its implementations, allowing consumers to swap the real AWS client for the in-memory version in test environments.

## Dependents (these services call us)

- **galleries**
  - REFERENCES_PACKAGE: package_name=DynamoDb.Fluent, version=1.0.0, confidence_band=high

## Projects

### DynamoDb.Fluent.Tests

DynamoDb.Fluent.Tests is the test project for the DynamoDb.Fluent library, providing both in-memory and real DynamoDB integration tests. It validates core fluent API operations — Put, Delete, Find, Query, and index-based queries — using two concrete context implementations: one backed by an in-memory store for fast unit tests and one backed by actual AWS DynamoDB for integration tests.

### DynamoDb.Fluent

DynamoDb.Fluent is a .NET library that provides a fluent, type-safe abstraction layer over Amazon DynamoDB. It exposes a unified interface for querying, scanning, putting, and deleting items from DynamoDB tables, and includes a complete in-memory implementation of the same interfaces to facilitate unit testing without requiring a live DynamoDB connection.

