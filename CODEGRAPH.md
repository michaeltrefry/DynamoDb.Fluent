# DynamoDb.Fluent

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-18 11:56 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

DynamoDb.Fluent is a .NET library that wraps the AWS DynamoDB SDK with a fluent, strongly-typed API. The repository contains two projects: the main library (DynamoDb.Fluent) and its test suite (DynamoDb.Fluent.Tests). The library provides a common interface hierarchy for DynamoDB table operations (put, delete, query, scan, index lookups) with two interchangeable implementations: a real DynamoDB backend using the AWS SDK, and an in-memory backend intended for testing and local development. Both implementations share the same interfaces, allowing consumers to swap between real and in-memory backends without changing application code. The test project validates both implementations using shared test logic against sample entity types, covering integration and unit scenarios. Externally, the library is consumed as a NuGet package by at least one known dependent repository ('galleries'), indicating it is distributed as a reusable infrastructure component for DynamoDB access in .NET applications.

## Dependents (these services call us)

- **galleries**
  - REFERENCES_PACKAGE: package_name=DynamoDb.Fluent, version=1.0.0, confidence_band=high

## Projects

### DynamoDb.Fluent

DynamoDb.Fluent is a .NET library that provides a fluent, strongly-typed API for interacting with AWS DynamoDB tables. It offers two interchangeable implementations: a real DynamoDB backend (via the AWS SDK) and an in-memory backend (useful for testing), both sharing the same interface hierarchy for table operations, queries, scans, and index lookups.

### DynamoDb.Fluent.Tests

DynamoDb.Fluent.Tests is the test project for the DynamoDb.Fluent library, containing integration and unit tests that validate the fluent API for interacting with DynamoDB. It tests both a real DynamoDB context (MyDynamoContext) and an in-memory context (MyMemoryDbContext) using shared test logic, covering operations like put, delete, index queries, and entity queries against sample entity types.

