# DynamoDb.Fluent

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-08 12:19 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

DynamoDb.Fluent is a .NET library that provides a fluent, type-safe abstraction layer over AWS DynamoDB. It exposes a unified interface hierarchy (IDynamoDbContext, ITable<T>, ITableIndex<T>, IObjectQuery<T>, IScanCondition<T>, IQueryCondition<T>) that supports chainable query building and CRUD operations (Put, Delete, Find, Query, Index queries). The library ships two backend implementations: a real DynamoDB backend built on the AWS SDK targeting production use, and an in-memory backend using JSON tokens and concurrent sorted dictionaries for fast, isolated testing without a live DynamoDB connection. The test project (DynamoDb.Fluent.Tests) exercises both backends, including integration tests against AWS us-east-1 and unit tests against the in-memory implementation. Because the two backends share the same interface contract, consumers can swap implementations without changing application code. An external repository ('galleries') references this library as a package dependency, indicating it is used as a reusable infrastructure component in at least one consuming application.

## Dependents (these services call us)

- **galleries**
  - REFERENCES_PACKAGE: package_name=DynamoDb.Fluent, version=1.0.0, confidence_band=high

## Projects

### DynamoDb.Fluent.Tests

DynamoDb.Fluent.Tests is the test project for the DynamoDb.Fluent library. It contains integration and unit tests that exercise the fluent API for interacting with DynamoDB tables, covering operations like Put, Delete, Find, Query, and Index queries. The project uses both a real DynamoDB context (targeting AWS us-east-1) and an in-memory context to enable fast, isolated unit testing without requiring a live DynamoDB connection.

### DynamoDb.Fluent

DynamoDb.Fluent is a .NET library that provides a fluent, type-safe abstraction layer over AWS DynamoDB, enabling query building, CRUD operations, and index access through chainable interfaces. It includes two implementations: a real DynamoDB backend (using the AWS SDK) and an in-memory backend (using JSON tokens and concurrent sorted dictionaries) suitable for testing. The library exposes a unified interface hierarchy (IDynamoDbContext, ITable<T>, ITableIndex<T>, IObjectQuery<T>, IScanCondition<T>, IQueryCondition<T>) so consumers can swap between real and in-memory backends without changing application code.

