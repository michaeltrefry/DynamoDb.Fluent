# DynamoDb.Fluent

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-09 00:35 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

DynamoDb.Fluent is a .NET library that provides a fluent, type-safe builder-pattern API for interacting with Amazon DynamoDB. The repository contains two projects: the core library (DynamoDb.Fluent) and its test suite (DynamoDb.Fluent.Tests). The core library abstracts DynamoDB operations (put, delete, find, query, index queries) behind a fluent interface and ships two backend implementations: a real DynamoDB client backed by the AWS SDK, and an in-memory simulation intended for local development and testing. The test project uses MSTest to run integration and unit tests against both backends (MyDynamoContext/DynamoDbContext for real DynamoDB and MyMemoryDbContext/MemoryDbContext for the in-memory simulation), verifying behavioral consistency between the two. An external repository named 'galleries' is a known consumer of this library, referencing it as a package dependency.

## Dependents (these services call us)

- **galleries**
  - REFERENCES_PACKAGE: package_name=DynamoDb.Fluent, version=1.0.0, confidence_band=high

## Projects

### DynamoDb.Fluent.Tests

DynamoDb.Fluent.Tests is the test project for the DynamoDb.Fluent library, containing MSTest-based integration and unit tests that verify the fluent API for DynamoDB table operations (put, delete, find, query, index queries). It exercises both a real DynamoDB backend (via MyDynamoContext/DynamoDbContext) and an in-memory backend (via MyMemoryDbContext/MemoryDbContext), validating that the two implementations behave consistently for CRUD and query operations against entity types like User and OwnedEntity.

### DynamoDb.Fluent

DynamoDb.Fluent is a library providing a fluent interface for interacting with Amazon DynamoDB and an in-memory simulation of DynamoDB. It allows developers to perform CRUD operations and complex queries using a type-safe, builder-pattern syntax that works across both real AWS environments and local memory-based testing implementations.

