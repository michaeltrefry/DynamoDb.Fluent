# DynamoDb.Fluent

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-09 12:54 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

DynamoDb.Fluent is a .NET library that provides a fluent, type-safe abstraction layer over AWS DynamoDB. The repository contains two projects: the core library (DynamoDb.Fluent) and its test suite (DynamoDb.Fluent.Tests). The core library exposes shared interfaces (IDynamoDbContext, ITable<T>, IObjectQuery<T>, etc.) and ships two backend implementations: a production implementation wrapping the AWS SDK's Document Model for real DynamoDB access, and an in-memory implementation using JSON tokens and concurrent sorted dictionaries for use in testing and local development. This dual-backend design allows application code to be written once against the shared interfaces and switched between real and in-memory storage without modification. The test project validates this behavior using MSTest-based integration and unit tests covering put, delete, query, GSI index queries, filtering, and pagination against both backends. The library is consumed as a package by at least one external repository ('galleries'), indicating it is distributed as a reusable NuGet package rather than application code.

## Dependents (these services call us)

- **galleries**
  - REFERENCES_PACKAGE: package_name=DynamoDb.Fluent, version=1.0.0, confidence_band=high

## Projects

### DynamoDb.Fluent.Tests

DynamoDb.Fluent.Tests is the test project for the DynamoDb.Fluent library. It contains MSTest-based integration and unit tests that validate fluent DynamoDB operations (put, delete, query, index queries) against both a real DynamoDB backend (via MyDynamoContext) and an in-memory simulation (via MyMemoryDbContext). The project exercises core table operations including primary/secondary key queries, GSI index queries, filtering, and pagination.

### DynamoDb.Fluent

DynamoDb.Fluent is a .NET library that provides a fluent, type-safe abstraction over AWS DynamoDB, exposing CRUD operations, query/scan builders, and index access through generic interfaces. It ships two implementations: a real DynamoDB backend (wrapping the AWS SDK's Document Model) and an in-memory backend (using JSON tokens and concurrent sorted dictionaries) intended for testing. The dual-backend design allows application code to be written against shared interfaces (IDynamoDbContext, ITable<T>, IObjectQuery<T>, etc.) and swapped between real and in-memory storage without code changes.

